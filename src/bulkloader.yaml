# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users
- import: bulk_helper

transformers:

- kind: Status
  connector: csv 
  connector_options:
      encoding: utf-8
      skip_import_header_row: true
  property_map:
    - property: __key__
      external_name: key
      import_template: "%(status_name)s"

    - property: status_name
      external_name: status_name

- kind: User
  connector: csv 
  connector_options:
      encoding: utf-8
      skip_import_header_row: true
      import_options:
          delimiter: "|"
  property_map:
    - property: __key__
      external_name: key
      import_template: "%(userid)s"

    - property: email
      external_name: email

    - property: nickname
      external_name: nickname

    - property: gravatar_icon_url
      external_name: gravatar_icon_url

    - property: created_date
      external_name: created_date
      import_transform: transform.import_date_time('%Y-%m-%d %H:%M:%S.%f')
      export_transform: transform.export_date_time('%Y-%m-%d %H:%M:%S.%f')

- kind: Item
  connector: csv 
  connector_options:
      encoding: utf-8
      skip_import_header_row: true
      import_options:
          delimiter: "|"
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_template: "%(item_id)s"

    - property: original_author
      external_name: original_author
      # Type: String Stats: 101 properties of this type in this kind.

    - property: class
      external_name: class
      export_transform: bulk_helper.list_to_json(str)
      import_transform: bulk_helper.comma_str_to_list(str)
      # Type: String Stats: 202 properties of this type in this kind.

    - property: content_html
      external_name: content_html
      # Type: Text Stats: 101 properties of this type in this kind.
      import_transform: db.Text

    - property: content_wmd
      external_name: content_wmd
      # Type: Text Stats: 101 properties of this type in this kind.
      import_transform: db.Text
      
    - property: created_date
      external_name: created_date
      # Type: Date/Time Stats: 100 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%d %H:%M:%S.%f')
      export_transform: transform.export_date_time('%Y-%m-%d %H:%M:%S.%f')

    - property: summary
      external_name: summary
      # Type: String Stats: 101 properties of this type in this kind.

    - property: title
      external_name: title
      # Type: String Stats: 101 properties of this type in this kind.

    - property: item_type
      external_name: item_type
      import_transform: int

    - property: hits
      external_name: hits
      import_transform: int

    - property: is_reviewable
      external_name: is_reviewable
      import_transform: bool
            
    - property: is_ratable
      external_name: is_ratable
      import_transform: bool

    #- property: rating
    #  external_name: rating
    #  import_transform: int
            
    - property: user
      external_name: user
      import_transform: transform.create_foreign_key('User')
            
    - property: status
      external_name: status
      import_transform: transform.create_foreign_key('Status')
      
    - property: icon_url
      external_name: icon_url

- kind: Review
  connector: csv 
  connector_options:
      encoding: utf-8
      skip_import_header_row: true
      import_options:
          delimiter: "|"
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: subject
      external_name: subject

    - property: ip_address
      external_name: ip_address

    - property: content_html
      external_name: content_html
      import_transform: db.Text
            
    - property: user
      external_name: user
      import_transform: transform.create_foreign_key('User')
            
    - property: item
      external_name: item
      import_transform: transform.create_foreign_key('Item')
      
    - property: created_date
      external_name: created_date
      # Type: Date/Time Stats: 100 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%d %H:%M:%S.%f')
      export_transform: transform.export_date_time('%Y-%m-%d %H:%M:%S.%f')


